#include <stdio.h>

// Without Perimeter & Return
// Input
// 10 60

void sum(void)
{
    int a, b;
    scanf("%d %d", &a, &b);
    int s = a + b;
    printf("%d", s);
}
int main()
{
    sum();
    return 0;
}